/**
 * Supabase integration stub.
 *
 * IMPORTANT: Do not put secrets here.
 * When you wire this up later, inject URL and anon key via runtime config.
 */
export function getSupabaseClient() {
  return null;
}
